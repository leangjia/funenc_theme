odoo.define('funenc.fnt_table_controller', function (require) {
    "use strict";

    /**
     * controller for fnt table
     */
    var core = require('web.core');
    var ListController = require('web.ListController');

    var _lt = core._lt;
    
    var fnt_table_controller = ListController.extend({

    });

    return fnt_table_controller;
});
